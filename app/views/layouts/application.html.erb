<!DOCTYPE html>
<html>
<head>
  <title>HomeSplash</title>

  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
  <link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
  <script src="https://code.jquery.com/jquery-1.11.2.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="clearance_links">
    <% if signed_in? %>
      Welcome <%= current_user.email %>!
      <%= link_to (fa_icon "sign-out", text: "Sign out"), sign_out_path, method: :delete %>
    <% else %>
      <%= link_to (fa_icon "sign-in", text: 'Sign in'), sign_in_path %>
      <%= link_to (fa_icon "users", text: 'Sign up'), sign_up_path %>
    <% end %>
  </div>

  <div id="flash">
    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>
  </div>

  <div class = "container-fluid">
    <%= yield %>
  </div>
  <%= render 'shared/footer' %>

</body>
</html>
